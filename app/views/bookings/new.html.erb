<%= simple_form_for([@studio, @booking]) do |f| %>
<%= f.error_notification %>
  <div class="form-inputs">
  <%= f.input :time_in %>
  <%= f.input :time_out %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
