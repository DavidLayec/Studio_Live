<main class="studio-container">

  <!-- Left Column / Headphones Image -->
  <div class="studio-left-column">
    <img src="https://source.unsplash.com/WK279Gj2G1c" />
  </div>

  <!-- Right Column -->
  <div class="studio-right-column">

    <!-- Product Description -->
    <div class="studio-description">
      <h1><%= @studio.title %></h1>
      <p><%= @studio.description %></p>
       <% if policy(@studio).destroy? %>
        <p><%= link_to "Delete my studio", studio_path(@studio), method: :delete, data: { confirm: "Are you sure?"}%></p>
      <% end %>
    </div>

    <!-- Product Pricing -->
    <div class="studio-price">
      <span>Only <%= @studio.price %> $</span>
      <p>The studio is located  <%= @studio.address %>  <%= @studio.zipcode %> in <%= @studio.city %> </p>
      <%= link_to new_studio_booking_path(@studio, @booking) do %>
      <button class="btn-flat">Book</button>
      <% end %>
    </div>
  </div>
</main>
